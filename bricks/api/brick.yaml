name: api
description: A new brick created with the Mason CLI.

version: 0.1.0+1
environment:
  mason: ">=0.1.0-dev.50 <0.1.0"

vars:
  name:
    type: string
    description: Your repo name (e.g. 'auth')
    default: home
    prompt: What is your repo's name?
