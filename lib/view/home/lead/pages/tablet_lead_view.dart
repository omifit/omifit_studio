import 'package:flutter/material.dart';

class TabletLeadView extends StatelessWidget {
  const TabletLeadView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
